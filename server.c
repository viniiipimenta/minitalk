#include <unistd.h>
#include <stdio.h>

int main(void)
{
    /*int     adress;
    char    printing;
    char    bits[9];

    bits[8] = '\0';
    adress = getpid();
    printf("%d\n", adress);

    if (bits[7] == '1')
			printing = printing + 1;
	if (bits[6] == '1')
		printing = printing + 2;
	if (bits[5] == '1')
		printing = printing + 4;
	if (bits[4] == '1')
		printing = printing + 8;
	if (bits[3] == '1')
		printing = printing + 16;
	if (bits[2] == '1')
		printing = printing + 32;
	if (bits[1] == '1')
		printing = printing + 64;
	if (bits[0] == '1')
		printing = printing + 128;
    printf("%s", printing);*/
    return (0);
}